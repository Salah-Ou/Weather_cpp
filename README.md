# Weather_cpp
A C++ weather station simulator built on the Observer design pattern. A WeatherData subject generates random temperature, humidity, and pressure readings, then notifies three live displays: current conditions (color-coded), running statistics (avg/min/max), and a simple temperature-based forecast.
